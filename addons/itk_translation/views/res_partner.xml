<odoo>
    <!-- Search-View -->
    <record id="res_partner_searchview_customization_itk" model="ir.ui.view">
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="model">res.partner</field>
        <field name="name">ITK: res.partner.select customization</field>
        <field name="priority">99</field>
        <field name="type">search</field>
        <field name="active" eval="True"/>
        <field name="arch" type="xml">
            <xpath expr="//search[1]/field[@name='name']" position="after">
                <field name="display_name"/>
                <field name="ref"/>
            </xpath>
            <xpath expr="//filter[@name='salesperson']" position="after">
                <filter context="{'group_by': 'ref'}" name="group_by_ref" string="Community Code"/>
                <filter context="{'group_by': 'status_of_partner_id'}" name="group_by_status" string="Status"/>
            </xpath>
        </field>
    </record>

    <!-- Tree-View -->
    <record id="view_partner_itk_tree" model="ir.ui.view">
        <field name="name">res.partner.tree (itk)</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='display_name']" position="before">
                <field name="ref" string="GKZ"/>
            </xpath>
            <xpath expr="//field[@name='email']" position="after">
                <field name="user_id" string="Salesperson"/>
                <field name="status_of_partner_id" string="Status"/>
            </xpath>
        </field>
    </record>

    <!-- Form-View -->
    <record id="view_partner_form_itk" model="ir.ui.view">
        <field name="name">res.partner.form (itk)</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='name']" position="before">
                <div style="width: 100%;">
                    <h1>
                        <field name="display_name"/>
                        <br/>
                    </h1>
                </div>
            </xpath>

            <xpath expr="//field[@name='name']" position="replace">
                <div class="o_edit_only">
                    <h3>
                        <field name="name"
                               invisible="is_company"/>
                    </h3>
                </div>
            </xpath>

            <xpath expr="//form[1]/sheet[1]/div[2]" position="after">
                <group name="group_1" string="Characteristics">
                    <group name="group_1_left">
                        <field name="ref" invisible="not is_company" string="GKZ"/>
                        <field name="attention_of"/>
                        <field name="community_salutation" invisible="not is_company"/>
                    </group>
                    <group name="group_1_right">
                        <field name="user_id" invisible="not is_company"/>
                        <field name="status_of_partner_id" invisible="not is_company"
                               widget="radio"/>
                    </group>
                </group>
            </xpath>

            <xpath expr="//form[1]/sheet[1]/group[2]" position="replace">
                <field name="type" invisible="not parent_id"
                       groups="base.group_no_one"/>
                <group>
                    <group>
                        <label for="street" string="Address"/>
                        <div class="o_address_format">
                            <field name="street" placeholder="Street..." class="o_address_street"
                                   readonly="type == 'contact' and parent_id"/>
                            <field name="street2" placeholder="Street 2..." class="o_address_street"
                                   readonly="type == 'contact' and parent_id"/>
                            <field name="zip" placeholder="ZIP" class="o_address_zip"
                                   readonly="type == 'contact' and parent_id"/>&amp;nbsp;
                            <field name="city" placeholder="City" class="o_address_city"
                                   readonly="type == 'contact' and parent_id"/>

                            <field name="state_id" class="o_address_state" placeholder="State"
                                   options="{&quot;no_open&quot;: True}"
                                   readonly="type == 'contact' and parent_id"
                                   context="{'country_id': country_id, 'zip': zip}"/>

                            <field name="country_id" placeholder="Country" class="o_address_country"
                                   options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"
                                   readonly="type == 'contact' and parent_id"/>
                        </div>
                        <field name="vat" placeholder="e.g. BE0477472701"
                               readonly="parent_id"/>
                        <field name="category_id" widget="many2many_tags"
                               options="{'color_field': 'color', 'no_create_edit': True}" placeholder="Tags..."/>
                    </group>
                    <group>
                        <field name="function" placeholder="e.g. Sales Director"
                               invisible="is_company"/>
                        <field name="phone" widget="phone"/>
                        <field name="mobile" widget="phone"/>
                        <field name="user_ids" invisible="1"/>
                        <field name="email" widget="email" context="{'gravatar_image': True}"
                               required="user_ids != []"/>
                        <field name="official_email" widget="email"
                               invisible="not is_company"/>
                        <field name="website" widget="url" placeholder="e.g. www.odoo.com"/>
                        <field name="salutation" invisible="is_company"/>
                        <field name="title_put_in_front" invisible="is_company"/>
                        <field name="title" options="{&quot;no_open&quot;: True}"
                               invisible="is_company"/>
                        <field name="title_put_in_back" invisible="is_company"/>
                        <field name="lang"/>
                    </group>
                </group>
            </xpath>

            <!-- Odoo 18: add reseller/user_id after website field -->
            <xpath expr="//field[@name='website']" position="after">
                <field name="reseller" invisible="not is_company"/>
                <field name="user_id" invisible="not is_company"/>
            </xpath>

            <notebook position="inside">
                <page invisible="not is_company" string="Community Information">
                    <group name="container_row_3">
                        <group string="Population" name="population">
                            <field name="population" invisible="not is_company"/>
                            <field name="community_magnitude"
                                   invisible="not is_company"/>
                            <field name="population_update"
                                   invisible="not is_company"/>
                        </group>
                        <group>
                            <group string="Other" name="other">
                                <field name="status_of_community"
                                       invisible="not is_company"/>
                                <field name="member_of_city_alliance"
                                       invisible="not is_company"/>
                            </group>
                        </group>
                    </group>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
